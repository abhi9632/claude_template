[Component name] in this project's style: one or two sentences on when to use it and
which variants exist.

```jsx
<ComponentName variant="primary">Label</ComponentName>
<ComponentName variant="secondary" size="sm">Label</ComponentName>
```

Props: `variant` ([list the variants]), `size` (sm | md | lg), plus any other props
(`disabled`, `href`, etc.). Follow the casing/voice rules in
`design-system/README.md` > Content rules for any copy inside the component.

Delete this file once real component specs exist, or keep it as the format reference
for new specs.
